# Archiv

Hier liegt Code, der vorübergehend aus der App entfernt wurde, aber später
überarbeitet und wieder aufgenommen werden soll (siehe `ROADMAP_QuizApp.md`,
Abschnitt 12). Dieser Ordner ist von `flutter analyze`/`flutter build`
ausgeschlossen (siehe `analysis_options.yaml`) und daher nicht Teil der App.

- `screens/whack_a_mole_screen.dart` – Whack-a-Mole-Modus, vorerst entfernt,
  soll später überarbeitet und wieder eingebaut werden.
